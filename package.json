{
  "name": "gudmo",
  "version": "0.4.2",
  "description": "Best-effort scheduler for tiny Codex keepalive prompts.",
  "type": "module",
  "bin": {
    "gudmo": "./bin/gudmo.js"
  },
  "scripts": {
    "check": "node --check bin/gudmo.js && node --check src/accounts.js && node --check src/cli.js && node --check src/cycle.js && node --check src/daemon.js && node --check src/eval.js && node --check src/rate-limits.js && node --check src/scheduler.js && node --check src/launchd.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": true
}
